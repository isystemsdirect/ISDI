# Firebase Hosting Deployment

This repository uses Firebase Hosting with multi-site configuration.

## Targets
- `public-site`: The main ISDI website.
- `overscite-preview`: The interactive OVERSCITE demo.

## Deployment Commands
Run `firebase deploy --only hosting:public-site` or `firebase deploy --only hosting:overscite-preview`.
